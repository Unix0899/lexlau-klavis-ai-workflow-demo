# SQL analysis results

> Synthetic demonstration data. No real client, legal-case or confidential LexLau/Klavis data is included.

Generated by `scripts/run_sql_analysis.py` from `sql/analysis_queries.sql` on `database/klavis_ai_demo.sqlite` (read-only).

## Q01 AI success rate, error rate and fallback rate (extraction runs)

```sql
SELECT extraction_runs, successful_runs, failed_runs, ai_success_rate_pct, error_rate_pct,
       fallback_runs, fallback_rate_pct
FROM vw_ai_quality_summary;
```

| extraction_runs | successful_runs | failed_runs | ai_success_rate_pct | error_rate_pct | fallback_runs | fallback_rate_pct |
|---|---|---|---|---|---|---|
| 349 | 320 | 29 | 91.7 | 8.3 | 34 | 9.7 |

## Q02 Extraction success by file type

```sql
SELECT file_family,
       COUNT(*)                                            AS runs,
       SUM(status = 'success')                             AS successful,
       ROUND(100.0 * SUM(status = 'success') / COUNT(*), 1) AS success_rate_pct,
       ROUND(AVG(processing_ms), 0)                        AS avg_processing_ms
FROM vw_extraction_runs
GROUP BY file_family
ORDER BY success_rate_pct;
```

| file_family | runs | successful | success_rate_pct | avg_processing_ms |
|---|---|---|---|---|
| DOCX | 129 | 103 | 79.8 | 928.0 |
| Image | 73 | 70 | 95.9 | 3205.0 |
| PDF | 147 | 147 | 100.0 | 1584.0 |

## Q03 DOCX success rate before and after the routing fix (16 Feb 2026)

```sql
SELECT CASE WHEN started_at < '2026-02-16T09:00:00' THEN '1. before fix' ELSE '2. after fix' END AS period,
       COUNT(*)                                             AS docx_runs,
       SUM(status = 'error' AND error_code = 'PARSE_ERROR') AS parse_errors,
       ROUND(100.0 * SUM(status = 'success') / COUNT(*), 1) AS success_rate_pct
FROM vw_extraction_runs
WHERE detected_format = 'docx'
GROUP BY period;
```

| period | docx_runs | parse_errors | success_rate_pct |
|---|---|---|---|
| 1. before fix | 25 | 25 | 0.0 |
| 2. after fix | 104 | 0 | 99.0 |

## Q04 Field completeness and accuracy by field (weakest first)

```sql
SELECT field_name, scored, accuracy_pct, completeness_pct, missing_rate_pct, manual_corrections,
       avg_confidence_when_found
FROM vw_field_accuracy
ORDER BY accuracy_pct;
```

| field_name | scored | accuracy_pct | completeness_pct | missing_rate_pct | manual_corrections | avg_confidence_when_found |
|---|---|---|---|---|---|---|
| important_dates | 320 | 87.8 | 100.0 | 0.0 | 27 | 0.894 |
| jurisdiction | 320 | 89.1 | 97.1 | 6.6 | 28 | 0.906 |
| case_title | 320 | 89.7 | 97.8 | 2.2 | 24 | 0.937 |
| amounts | 320 | 91.9 | 97.1 | 4.4 | 21 | 0.929 |
| opposing_party | 320 | 92.5 | 97.1 | 4.7 | 15 | 0.936 |
| client_name | 320 | 94.1 | 97.8 | 2.2 | 12 | 0.936 |
| document_type | 320 | 96.3 | 96.3 | 3.8 | 9 | 0.914 |
| case_reference | 320 | 96.6 | 97.5 | 2.5 | 10 | 0.937 |
| case_category | 320 | 98.1 | 100.0 | 0.0 | 4 | 0.897 |

## Q05 Average confidence by document variant, with a rank (window function)

```sql
SELECT sample_variant,
       COUNT(*)                          AS successful_runs,
       ROUND(AVG(confidence_score), 3)   AS avg_confidence,
       RANK() OVER (ORDER BY AVG(confidence_score) DESC) AS confidence_rank
FROM vw_extraction_runs
WHERE status = 'success'
GROUP BY sample_variant;
```

| sample_variant | successful_runs | avg_confidence | confidence_rank |
|---|---|---|---|
| clean | 198 | 0.931 | 1 |
| multipage | 18 | 0.926 | 2 |
| contradictory | 20 | 0.89 | 3 |
| missing_fields | 27 | 0.825 | 4 |
| degraded | 57 | 0.789 | 5 |

## Q06 Is confidence a useful signal? Field accuracy by confidence band

```sql
SELECT CASE WHEN confidence_score >= 0.90 THEN '1. >= 0.90'
            WHEN confidence_score >= 0.80 THEN '2. 0.80-0.89'
            WHEN confidence_score >= 0.70 THEN '3. 0.70-0.79'
            ELSE '4. < 0.70' END                   AS confidence_band,
       COUNT(DISTINCT e.ai_run_id)                AS runs,
       ROUND(100.0 * SUM(f.is_correct) / COUNT(f.is_correct), 1) AS field_accuracy_pct,
       ROUND(100.0 * SUM(f.was_corrected) / COUNT(f.was_corrected), 1) AS manual_correction_rate_pct
FROM vw_extraction_runs e
JOIN extracted_fields f ON f.ai_run_id = e.ai_run_id
WHERE e.status = 'success'
GROUP BY confidence_band
ORDER BY confidence_band;
```

| confidence_band | runs | field_accuracy_pct | manual_correction_rate_pct |
|---|---|---|---|
| 1. >= 0.90 | 212 | 98.4 | 1.7 |
| 2. 0.80-0.89 | 67 | 89.9 | 8.7 |
| 3. 0.70-0.79 | 31 | 74.2 | 21.8 |
| 4. < 0.70 | 10 | 54.4 | 46.0 |

## Q07 Manual correction rate by document type

```sql
SELECT file_family, sample_variant, manual_correction_rate_pct, field_accuracy_pct, human_review_rate_pct
FROM vw_document_type_performance
ORDER BY manual_correction_rate_pct DESC;
```

| file_family | sample_variant | manual_correction_rate_pct | field_accuracy_pct | human_review_rate_pct |
|---|---|---|---|---|
| Image | degraded | 42.9 | 57.1 | 100.0 |
| Image | degraded | 36.4 | 63.0 | 66.7 |
| PDF | degraded | 29.5 | 68.3 | 62.9 |
| DOCX | contradictory | 11.1 | 88.9 | 100.0 |
| PDF | contradictory | 11.1 | 88.9 | 100.0 |
| Image | clean | 1.4 | 99.4 | 0.0 |
| PDF | missing_fields | 1.2 | 98.8 | 100.0 |
| PDF | multipage | 0.7 | 99.4 | 5.6 |
| Image | clean | 0.5 | 99.6 | 4.0 |
| PDF | clean | 0.4 | 99.7 | 0.0 |
| DOCX | clean | 0.2 | 99.7 | 1.3 |
| DOCX | missing_fields | 0.0 | 100.0 | 100.0 |
| Image | missing_fields | 0.0 | 100.0 | 100.0 |

## Q08 Error analysis: where runs fail and whether a retry recovered them

```sql
SELECT task, error_stage, error_code, detected_format, failed_runs, recovered_by_retry, first_seen, last_seen
FROM vw_error_analysis
ORDER BY failed_runs DESC;
```

| task | error_stage | error_code | detected_format | failed_runs | recovered_by_retry | first_seen | last_seen |
|---|---|---|---|---|---|---|---|
| extraction | text_extraction | PARSE_ERROR | docx | 25 | 24 | 2026-01-07T15:41:53 | 2026-02-13T08:21:34 |
| extraction | ai_provider | AI_UNAVAILABLE | jpg | 2 | 1 | 2026-01-28T12:06:40 | 2026-03-15T17:43:08 |
| extraction | ai_provider | AI_UNAVAILABLE | docx | 1 | 1 | 2026-06-09T14:10:08 | 2026-06-09T14:10:08 |
| extraction | ai_provider | AI_UNAVAILABLE | png | 1 | 1 | 2026-01-12T12:07:10 | 2026-01-12T12:07:10 |

## Q09 Fallback: why the primary failed, and the quality of what the fallback returned

```sql
SELECT fallback_reason,
       COUNT(*)                                             AS runs,
       SUM(status = 'success')                              AS rescued_by_fallback,
       ROUND(AVG(CASE WHEN status = 'success' THEN confidence_score END), 3) AS avg_confidence
FROM vw_extraction_runs
WHERE fallback_used = 1
GROUP BY fallback_reason
ORDER BY runs DESC;
```

| fallback_reason | runs | rescued_by_fallback | avg_confidence |
|---|---|---|---|
| PROVIDER_TIMEOUT | 13 | 13 | 0.76 |
| INVALID_OUTPUT | 11 | 11 | 0.835 |
| PROVIDER_ERROR | 10 | 6 | 0.835 |

## Q10 Provider performance

```sql
SELECT COALESCE(provider_used, 'none (both failed)') AS provider,
       COUNT(*)                                     AS runs,
       ROUND(AVG(confidence_score), 3)              AS avg_confidence,
       ROUND(AVG(processing_ms), 0)                 AS avg_processing_ms,
       ROUND(100.0 * SUM(intake_status = 'Complete') / COUNT(*), 1) AS complete_pct
FROM vw_extraction_runs
GROUP BY provider
ORDER BY runs DESC;
```

| provider | runs | avg_confidence | avg_processing_ms | complete_pct |
|---|---|---|---|---|
| mock-primary | 290 | 0.903 | 1410.0 | 73.4 |
| mock-fallback | 30 | 0.803 | 4573.0 | 60.0 |
| none (both failed) | 29 |  | 1392.0 |  |

## Q11 Field accuracy by provider (same documents engine vs simpler fallback)

```sql
SELECT e.provider_used, COUNT(f.is_correct) AS scored_fields,
       ROUND(100.0 * SUM(f.is_correct) / COUNT(f.is_correct), 1) AS field_accuracy_pct
FROM vw_extraction_runs e
JOIN extracted_fields f ON f.ai_run_id = e.ai_run_id
WHERE e.status = 'success'
GROUP BY e.provider_used;
```

| provider_used | scored_fields | field_accuracy_pct |
|---|---|---|
| mock-fallback | 270 | 89.6 |
| mock-primary | 2610 | 93.2 |

## Q12 Average processing time by format (latency model, see data dictionary)

```sql
SELECT detected_format, COUNT(*) AS runs, ROUND(AVG(processing_ms), 0) AS avg_ms,
       ROUND(MIN(processing_ms), 0) AS min_ms, ROUND(MAX(processing_ms), 0) AS max_ms
FROM vw_extraction_runs
GROUP BY detected_format
ORDER BY avg_ms;
```

| detected_format | runs | avg_ms | min_ms | max_ms |
|---|---|---|---|---|
| docx | 129 | 928.0 | 501.0 | 6833.0 |
| pdf | 147 | 1584.0 | 683.0 | 8341.0 |
| png | 36 | 3043.0 | 1841.0 | 8814.0 |
| jpg | 37 | 3363.0 | 2016.0 | 8144.0 |

## Q13 Cases requiring human review, by intake status

```sql
SELECT intake_status, COUNT(*) AS cases,
       ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS share_pct,
       SUM(human_review_status = 'Approved with corrections') AS corrected_by_reviewer
FROM cases
GROUP BY intake_status
ORDER BY cases DESC;
```

| intake_status | cases | share_pct | corrected_by_reviewer |
|---|---|---|---|
| Complete | 182 | 70.0 | 13 |
| Missing Information | 54 | 20.8 | 30 |
| Needs Review | 24 | 9.2 | 22 |

## Q14 Missing-field frequency (successful primary extractions)

```sql
WITH missing AS (
    SELECT f.field_name, COUNT(*) AS missing_runs
    FROM extracted_fields f JOIN vw_extraction_runs e ON e.ai_run_id = f.ai_run_id
    WHERE f.is_missing = 1 AND e.status = 'success'
    GROUP BY f.field_name
)
SELECT field_name, missing_runs,
       ROUND(100.0 * missing_runs / (SELECT COUNT(*) FROM vw_extraction_runs WHERE status = 'success'), 1)
           AS missing_rate_pct,
       SUM(missing_runs) OVER (ORDER BY missing_runs DESC ROWS UNBOUNDED PRECEDING) AS cumulative_missing
FROM missing
ORDER BY missing_runs DESC;
```

| field_name | missing_runs | missing_rate_pct | cumulative_missing |
|---|---|---|---|
| jurisdiction | 21 | 6.6 | 21 |
| opposing_party | 15 | 4.7 | 36 |
| amounts | 14 | 4.4 | 50 |
| document_type | 12 | 3.8 | 62 |
| case_reference | 8 | 2.5 | 70 |
| case_title | 7 | 2.2 | 77 |
| client_name | 7 | 2.2 | 84 |

## Q15 Missing because the document lacks it vs missed by the extraction

```sql
SELECT f.field_name,
       SUM(f.is_missing = 1 AND f.expected_value IS NULL)     AS correctly_reported_missing,
       SUM(f.is_missing = 1 AND f.expected_value IS NOT NULL) AS missed_by_extraction
FROM extracted_fields f
GROUP BY f.field_name
HAVING SUM(f.is_missing) > 0
ORDER BY missed_by_extraction DESC;
```

| field_name | correctly_reported_missing | missed_by_extraction |
|---|---|---|
| document_type | 0 | 12 |
| opposing_party | 6 | 9 |
| jurisdiction | 12 | 9 |
| amounts | 5 | 9 |
| case_reference | 0 | 8 |
| client_name | 0 | 7 |
| case_title | 0 | 7 |

## Q16 Test pass rate per run and regressions

```sql
SELECT test_run_id, run_label, tests_passed || '/' || tests_total AS passed, pass_rate_pct,
       regressions, COALESCE(failed_scenarios, '-') AS failed_scenarios
FROM vw_test_run_summary
ORDER BY test_run_id;
```

| test_run_id | run_label | passed | pass_rate_pct | regressions | failed_scenarios |
|---|---|---|---|---|---|
| 1 | v0.4 baseline - bug replay (5 synthetic bugs) | 7/14 | 50.0 | 0 | TEST 002, TEST 005, TEST 006, TEST 007, TEST 009, TEST 013, TEST 014 |
| 2 | fix/docx-content-routing | 10/14 | 71.4 | 0 | TEST 005, TEST 007, TEST 009, TEST 014 |
| 3 | fix/category-word-boundary | 11/14 | 78.6 | 0 | TEST 005, TEST 007, TEST 009 |
| 4 | fix/missing-field-null | 12/14 | 85.7 | 0 | TEST 007, TEST 009 |
| 5 | merge feature/assistant-intents (regression caught) | 8/14 | 57.1 | 4 | TEST 002, TEST 006, TEST 007, TEST 009, TEST 013, TEST 014 |
| 6 | revert routing + fix/fallback-provider-label | 13/14 | 92.9 | 0 | TEST 009 |
| 7 | fix/idempotent-case-creation | 14/14 | 100.0 | 0 | - |
| 8 | release candidate v1.0 - full regression | 14/14 | 100.0 | 0 | - |

## Q17 Regression failures (a scenario that passed in the previous run and fails now)

```sql
SELECT t.run_label, r.scenario_code, r.scenario_name, r.failure_category, r.observed
FROM test_results r JOIN test_runs t USING (test_run_id)
WHERE r.is_regression = 1
ORDER BY t.test_run_id, r.scenario_code;
```

| run_label | scenario_code | scenario_name | failure_category | observed |
|---|---|---|---|---|
| merge feature/assistant-intents (regression caught) | TEST 002 | DOCX | document_routing | status=error error=PARSE_ERROR stage=text_extraction |
| merge feature/assistant-intents (regression caught) | TEST 006 | Multiple dates | date_normalisation | status=error error=PARSE_ERROR |
| merge feature/assistant-intents (regression caught) | TEST 013 | Mixed document formats | document_routing | failed formats: docx |
| merge feature/assistant-intents (regression caught) | TEST 014 | Regression suite | regression | diffs: clean_letter.docx:PARSE_ERROR, multiple_dates.docx:PARSE_ERROR |

## Q18 Scenario stability across all runs

```sql
SELECT scenario_code, scenario_name,
       SUM(status = 'PASS') || '/' || COUNT(*) AS passed_runs,
       MIN(CASE WHEN status = 'FAIL' THEN test_run_id END) AS first_failed_run,
       MAX(CASE WHEN status = 'FAIL' THEN test_run_id END) AS last_failed_run
FROM test_results
GROUP BY scenario_code, scenario_name
ORDER BY scenario_code;
```

| scenario_code | scenario_name | passed_runs | first_failed_run | last_failed_run |
|---|---|---|---|---|
| TEST 001 | Clean PDF | 8/8 |  |  |
| TEST 002 | DOCX | 6/8 | 1 | 5 |
| TEST 003 | Clean image | 8/8 |  |  |
| TEST 004 | Degraded image | 8/8 |  |  |
| TEST 005 | Missing field | 5/8 | 1 | 3 |
| TEST 006 | Multiple dates | 6/8 | 1 | 5 |
| TEST 007 | Provider failure | 3/8 | 1 | 5 |
| TEST 008 | Both providers fail | 8/8 |  |  |
| TEST 009 | Duplicate submission | 2/8 | 1 | 6 |
| TEST 010 | Human correction | 8/8 |  |  |
| TEST 011 | Invalid file | 8/8 |  |  |
| TEST 012 | Oversized file | 8/8 |  |  |
| TEST 013 | Mixed document formats | 6/8 | 1 | 5 |
| TEST 014 | Regression suite | 5/8 | 1 | 5 |

## Q19 Create Case success rate: validated intakes that became a case

```sql
SELECT COUNT(*)                                        AS successful_primary_extractions,
       SUM(case_id IS NOT NULL)                         AS turned_into_case,
       ROUND(100.0 * SUM(case_id IS NOT NULL) / COUNT(*), 1) AS create_case_success_pct
FROM vw_extraction_runs e
WHERE e.status = 'success'
  AND EXISTS (SELECT 1 FROM documents d WHERE d.document_id = e.document_id AND d.document_role = 'primary');
```

| successful_primary_extractions | turned_into_case | create_case_success_pct |
|---|---|---|
| 272 | 260 | 95.6 |

## Q20 Duplicate prevention

```sql
SELECT event_type, COUNT(*) AS events
FROM audit_events
WHERE event_type IN ('duplicate_case_blocked', 'duplicate_submission_ignored', 'case_created')
GROUP BY event_type;
```

| event_type | events |
|---|---|
| case_created | 260 |
| duplicate_case_blocked | 12 |
| duplicate_submission_ignored | 3 |

## Q21 Proof that no duplicate reference exists

```sql
SELECT COUNT(*) AS cases, COUNT(DISTINCT reference_key) AS distinct_references,
       COUNT(*) - COUNT(DISTINCT reference_key) AS duplicates
FROM cases;
```

| cases | distinct_references | duplicates |
|---|---|---|
| 260 | 260 | 0 |

## Q22 Monthly trend with running totals (window functions)

```sql
SELECT substr(started_at, 1, 7)                          AS month,
       COUNT(*)                                          AS runs,
       ROUND(100.0 * SUM(status = 'success') / COUNT(*), 1) AS success_rate_pct,
       SUM(COUNT(*)) OVER (ORDER BY substr(started_at, 1, 7)) AS cumulative_runs,
       ROUND(AVG(AVG(confidence_score)) OVER (ORDER BY substr(started_at, 1, 7)
             ROWS BETWEEN 2 PRECEDING AND CURRENT ROW), 3) AS confidence_3m_avg
FROM vw_extraction_runs
GROUP BY month
ORDER BY month;
```

| month | runs | success_rate_pct | cumulative_runs | confidence_3m_avg |
|---|---|---|---|---|
| 2026-01 | 49 | 65.3 | 49 | 0.885 |
| 2026-02 | 60 | 83.3 | 109 | 0.894 |
| 2026-03 | 59 | 98.3 | 168 | 0.892 |
| 2026-04 | 56 | 100.0 | 224 | 0.896 |
| 2026-05 | 66 | 100.0 | 290 | 0.89 |
| 2026-06 | 58 | 98.3 | 348 | 0.895 |
| 2026-07 | 1 | 100.0 | 349 | 0.912 |

## Q23 Category corrections before and after the word-boundary fix (9 Mar 2026)

```sql
SELECT CASE WHEN e.started_at < '2026-03-09T09:00:00' THEN '1. before fix' ELSE '2. after fix' END AS period,
       COUNT(f.was_corrected)                          AS reviewed_categories,
       SUM(f.was_corrected)                            AS category_corrections
FROM extracted_fields f JOIN vw_extraction_runs e ON e.ai_run_id = f.ai_run_id
WHERE f.field_name = 'case_category'
GROUP BY period;
```

| period | reviewed_categories | category_corrections |
|---|---|---|
| 1. before fix | 90 | 4 |
| 2. after fix | 170 | 0 |

## Q24 Reviewer workload and correction behaviour (simulated reviewers)

```sql
SELECT reviewer, COUNT(*) AS reviews, SUM(fields_corrected) AS fields_corrected,
       ROUND(AVG(review_seconds), 0) AS avg_review_seconds
FROM human_reviews
WHERE review_type = 'intake_validation'
GROUP BY reviewer
ORDER BY reviews DESC;
```

| reviewer | reviews | fields_corrected | avg_review_seconds |
|---|---|---|---|
| reviewer_03 | 71 | 39 | 70.0 |
| reviewer_04 | 65 | 41 | 71.0 |
| reviewer_02 | 64 | 28 | 68.0 |
| reviewer_01 | 60 | 42 | 74.0 |

## Q25 Product feedback generated by reviewer corrections

```sql
SELECT feedback_type, field_name, COUNT(*) AS occurrences
FROM ai_feedback
GROUP BY feedback_type, field_name
ORDER BY occurrences DESC
LIMIT 12;
```

| feedback_type | field_name | occurrences |
|---|---|---|
| wrong_value | important_dates | 23 |
| wrong_value | case_title | 19 |
| wrong_value | jurisdiction | 11 |
| conflict_resolved | jurisdiction | 9 |
| missing_value | document_type | 9 |
| wrong_value | amounts | 9 |
| wrong_value | opposing_party | 9 |
| missing_value | case_reference | 8 |
| missing_value | jurisdiction | 8 |
| wrong_value | client_name | 8 |
| conflict_resolved | amounts | 6 |
| missing_value | amounts | 6 |

## Q26 Audit trail content check: events contain metadata only

```sql
SELECT event_type, COUNT(*) AS events, MAX(LENGTH(COALESCE(details, ''))) AS max_details_chars
FROM audit_events
GROUP BY event_type
ORDER BY events DESC;
```

| event_type | events | max_details_chars |
|---|---|---|
| document_uploaded | 357 | 22 |
| ai_extraction_completed | 320 | 106 |
| human_review_submitted | 260 | 63 |
| case_created | 260 | 0 |
| summary_generated | 133 | 0 |
| case_updated | 93 | 23 |
| assistant_question | 90 | 0 |
| provider_fallback_used | 30 | 39 |
| ai_extraction_failed | 29 | 24 |
| duplicate_case_blocked | 12 | 21 |
| test_run_completed | 8 | 150 |
| file_rejected | 8 | 0 |
| duplicate_submission_ignored | 3 | 18 |
